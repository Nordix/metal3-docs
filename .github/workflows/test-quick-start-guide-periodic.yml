name: Test Quick Start Guide Periodic

on:
  schedule:
  - cron: "0 0 * * *"

permissions: {}

jobs:
  periodic-e2e-test:
    if: github.repository == 'metal3-io/metal3-docs'
    uses: ./.github/workflows/test-quick-start-guide.yml
    with:
      runner: "ubuntu-latest"
      timeout-minutes: 90
    permissions:
      contents: read
